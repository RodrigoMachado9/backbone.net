##
# backbone.net — Variáveis de ambiente
#
# Copie este arquivo e ajuste os caminhos para seu ambiente:
#   cp .env.example .env
#
# O Makefile carrega automaticamente as variáveis deste arquivo.
##

# Caminho da ISO VyOS (obrigatório)
VYOS_ISO=/home/SEU_USUARIO/lab-backbone/iso/vyos-rolling-latest.iso

# Caminho da imagem FortiGate QCOW2 (necessário a partir da Fase 05)
# FORTIGATE_IMAGE=/home/SEU_USUARIO/lab-backbone/images/fortios.qcow2

# Diretório para discos das VMs (usado pelo deploy-vms.sh)
# DISK_DIR=/home/SEU_USUARIO/lab-backbone/disks

# RAM por VM VyOS em MB (padrão: 512)
# RAM_MB=512
